# Cassandra on Docker
#
# A thin re-tag of the upstream multi-arch cassandra image, providing a
# stable hook for layering on local customisations (JVM opts, snitch
# config, etc.) without pinning every script to upstream's tag scheme.
FROM cassandra:5
